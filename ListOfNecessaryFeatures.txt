I need a class fmGlobalParameter
where exists all parameters {A, Dp, rho_f, ...}
exists container<fmGlobalParameter> parameters -- for iterating them (for copying for example)
exists container<string, fmGlobalParameter> parametersByName -- for getting parameter object from name


I need a function GetParametersListThatCanBeInput(calcluation Option)
for a fmFilterMachiningBlock.CalculationOption
for a fmFilterSimulation.CalculationOption (it is complex parameter of all options of sub blocks)